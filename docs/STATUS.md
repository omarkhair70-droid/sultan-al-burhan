# Status

The `site-rebuild` branch is the candidate for the first stable GitHub-backed release.

Current state:
- Site source lives in one repository.
- Bootstrap markers are removed on the rebuild branch.
- The main remaining release blockers are downloadable binary assets and final preview verification.
